/**
 * client/src/config/constants.js
 *
 * Shared browser-side constants.
 * Keep these values in sync with the server-side role definitions.
 */

export const ROLES = {
    ADMIN: 'admin',
    OPERATIONS: 'operations',
    ACCOUNTS: 'accounts',
    DRIVER: 'driver',
};
