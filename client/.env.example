# Example client environment variables for LogiTrack
# Vite exposes only VITE_ prefixed variables to the browser.
# For local development, you can leave this unset to use the Vite proxy.
# For production deployments, set this to your Render backend URL.

VITE_API_URL=https://your-render-backend-url/api
